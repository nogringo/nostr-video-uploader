name: nostr_video_uploader
description: "A cross platform video uploader for Nostr."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  file_picker: ^10.3.3
  flutter:
    sdk: flutter
  get: ^4.7.2
  image: ^4.5.4
  intl: ^0.20.2
  media_kit: ^1.2.1
  ndk: ^0.5.1
  nostr_widgets: ^1.1.0
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
